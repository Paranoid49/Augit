# 第三方依赖说明

本文件记录 Augit 首版直接依赖的用途、运行时成本、替代方案、许可证和移除条件，实际分发时还必须附带依赖原始许可证文本。

## Scintilla 5.6.6

- 用途：通过原生 Win32 控件提供只读文本、行号、选择、复制、查找、跳转行、自动换行、空白符显示和 diff；只有三栏冲突解决器的结果区允许解除只读。
- 运行时成本：分发一个 `1128808` 字节的 x64 `Scintilla.dll`，仅在首次创建文本、diff 或冲突控件时加载；不分发 Lexilla，不启用语法高亮。
- 替代方案：Windows RichEdit 无法同时稳定满足行号、空白符显示和 diff；自研 DirectWrite 文本控件会显著增加实现与维护成本；ScintillaNET 会引入 WinForms 和 Desktop Runtime。
- 许可证：Scintilla 宽松许可证。
- 移除条件：原生系统控件能够在相同功能和性能下完整替代，或者 Scintilla 无法维持只读边界、启动或内存指标。

## Markdig 0.41.3

- 用途：解析标准 Markdown、表格、任务列表、删除线和自动链接；原生 HTML 被禁用。
- 运行时成本：仅在打开 Markdown 预览时加载并生成内存中的受控 HTML。
- 替代方案：自行实现 Markdown 解析器风险高且超出项目范围；其他解析器需要重新验证语法和性能。
- 许可证：BSD-2-Clause。
- 移除条件：Markdown 预览被取消，或者出现更轻且兼容当前语法范围的宽松许可证实现。

## Microsoft.Web.WebView2 1.0.3650.58

- 用途：按需承载 Markdown 预览与 xterm.js 终端界面。
- 运行时成本：创建预览或终端时启动 WebView2 进程并增加内存；普通启动和文本浏览不加载。
- 替代方案：原生 Win32 没有满足安全边界的 HTML 与终端渲染控件；自行实现浏览器或终端渲染不可接受。
- 许可证：BSD-3-Clause。
- 移除条件：Markdown 与内置终端均不再依赖网页渲染，或者出现满足安全和兼容要求的更轻方案。

## xterm.js 6.0.0

- 用途：在按需创建的终端 WebView2 中处理 ANSI 控制序列、键盘输入、选择复制和 2000 行滚动缓冲，不承担 Shell 或进程管理。
- 运行时成本：发布产物增加 `488663` 字节的 `xterm.js` 和 `7112` 字节的样式文件；仅在用户打开内置终端时由独立 WebView2 环境加载，关闭后结束该环境的浏览器进程。
- 替代方案：自行实现终端协议和渲染会显著做重并增加兼容风险；直接嵌入 Windows Terminal 没有稳定的受支持宿主接口。
- 许可证：MIT。
- 移除条件：内置终端被取消，或者 Windows 提供可嵌入、同等兼容且资源成本更低的稳定原生终端控件。

## @xterm/addon-fit 0.11.0

- 用途：按 WebView2 宿主尺寸计算终端行列并触发 ConPTY 尺寸同步，避免依赖 xterm.js 私有接口。
- 运行时成本：发布产物增加 `1521` 字节的按需 JavaScript，不创建进程、不常驻后台。
- 替代方案：自行按字体测量估算行列会产生缩放和高 DPI 偏差；调用 xterm.js 私有接口会使锁定版本升级不稳定。
- 许可证：MIT。
- 移除条件：xterm.js 提供稳定的内置尺寸适配接口，或者终端宿主不再需要动态调整尺寸。

## ripgrep 15.2.0

- 用途：按需执行文件名搜索和全仓文本搜索，不建立索引。
- 运行时成本：仅在搜索期间创建独立 `rg.exe` 进程，取消、超时或达到结果上限后立即结束。
- 替代方案：自行递归搜索会增加复杂度和性能风险；Windows Search 无法保证仓库语义和可移植性。
- 许可证：MIT 或 Unlicense 双许可证。
- 移除条件：无法满足进程回收或性能指标，或者平台提供同等语义且更轻的稳定能力。

## MSTest 4.0.2

- 用途：执行核心规则、基础设施和原生 Win32 烟雾测试。
- 运行时成本：仅用于开发和测试，不进入发布产物。
- 替代方案：xUnit.net 或 NUnit，需要迁移测试与重新验证工具链。
- 许可证：MIT。
- 移除条件：项目统一迁移到其他受支持的测试框架。

## Inno Setup 7.1.0

- 用途：仅在发布构建时生成 Windows 小型联网安装器，并实现可选 PATH、资源管理器入口、运行时检测和卸载；通过 `Tools.InnoSetup` NuGet 包锁定编译器版本。
- 运行时成本：不进入便携版运行路径，也不创建常驻进程；安装器自身包含 Inno Setup 引擎，构建机额外下载约 21 MB 的工具包。
- 替代方案：MSIX 不适合当前可选系统 PATH、资源管理器入口和缺失运行时安装流程；WiX 会增加工具链和安装定义复杂度；自研安装器风险与维护成本更高。
- 许可证：Inno Setup License；`Tools.InnoSetup` 包的其他支持文件声明为公有领域。
- 移除条件：不再提供安装版，或者 Windows 官方安装技术能以更低构建与维护成本完整覆盖当前安装行为。
